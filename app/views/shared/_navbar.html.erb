<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav navbar-brand ml-auto mx-5">
        <% if user_signed_in? %>
          <li class="d-flex mb-3">
            <button class = "btn btn-outline-secondary btn-lg p-2 nav-link"><i class="bi bi-eye-fill"></i> ANÁLISES</button>
            <button class = "btn btn-outline-secondary btn-lg p-2 mx-4 nav-link"><i class="bi bi-arrow-left-right"></i> ENTRADAS & SAÍDAS</button>
            <%= button_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'btn btn-light btn-outline-secondary btn-lg p-2 nav-link' %>
          </li>
        <% else %>
            <li class="d-flex mb-3">
                <%= link_to 'Sign In', new_user_session_path, class: 'btn btn-outline-secondary btn-lg p-2 nav-link' %> 
                <%= link_to 'Register', new_user_registration_path, class: 'btn btn-outline-secondary btn-lg p-2 nav-link' %>
            </li>
        <% end %>
    </ul>
  </div>
</nav>